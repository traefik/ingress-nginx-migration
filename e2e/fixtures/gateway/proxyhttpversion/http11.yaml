---
# proxy-http-version has no Gateway API equivalent; plain HTTPRoute used.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gateway-http-version-11-test
  namespace: default
spec:
  parentRefs:
    - name: traefik
  hostnames:
    - http-version-11-test.gateway.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: backend
          port: 80
