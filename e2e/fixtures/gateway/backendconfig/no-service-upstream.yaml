---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gateway-no-service-upstream-test
  namespace: default
spec:
  parentRefs:
    - name: traefik
  hostnames:
    - no-service-upstream-test.gateway.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: backend
          port: 80
