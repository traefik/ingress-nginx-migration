---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: gateway-body-limit-test-bodylimit
  namespace: default
spec:
  buffering:
    maxRequestBodyBytes: 1024

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gateway-body-limit-test
  namespace: default
spec:
  parentRefs:
    - name: traefik
  hostnames:
    - body-limit-test.gateway.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: gateway-body-limit-test-bodylimit
      backendRefs:
        - name: backend
          port: 80
