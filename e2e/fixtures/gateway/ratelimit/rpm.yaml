---
# Migrated from ingress-nginx limit-rpm: "60"
# period: 1m so average expresses requests per minute.
# burst = average * limit-burst-multiplier (default 5) = 60 * 5 = 300
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: gateway-ratelimit-rpm-test-ratelimit
  namespace: default
spec:
  rateLimit:
    average: 60
    period: 1m
    burst: 300
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gateway-ratelimit-rpm-test
  namespace: default
spec:
  parentRefs:
    - name: traefik
  hostnames:
    - ratelimit-rpm-test.gateway.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: gateway-ratelimit-rpm-test-ratelimit
      backendRefs:
        - name: backend
          port: 80
