---
# Migrated from: backend-protocol: HTTPS + proxy-ssl-secret
# HTTPRoute only: BackendTLSPolicy is in backend-tls-policy.yaml (shared per service).
# backendRefs[].port is set to the HTTPS port (443) per backend-protocol: HTTPS.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gateway-proxy-ssl-basic-test
  namespace: default
spec:
  parentRefs:
    - name: traefik
  hostnames:
    - proxy-ssl-basic-test.gateway.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: https-backend
          port: 443
