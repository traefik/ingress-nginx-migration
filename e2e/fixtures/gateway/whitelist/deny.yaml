---
# Migrated: whitelist-source-range: "192.0.2.0/24" -> IPAllowList (TEST-NET-1, will deny test client)
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: gateway-whitelist-deny-test-ipallow
  namespace: default
spec:
  ipAllowList:
    sourceRange:
      - "192.0.2.0/24"
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gateway-whitelist-deny-test
  namespace: default
spec:
  parentRefs:
    - name: traefik
  hostnames:
    - whitelist-deny-test.gateway.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: gateway-whitelist-deny-test-ipallow
      backendRefs:
        - name: backend
          port: 80
