---
# MIGRATION GAP - per-route timeouts not supported in Traefik Gateway API provider.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gateway-timeout-send-only-test
  namespace: default
spec:
  parentRefs:
    - name: traefik
  hostnames:
    - timeout-send-only-test.gateway.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: backend
          port: 80
